# Native shell build output (CMake + Ninja + vcpkg)
native/buildRelease/
native/build/
native/out/

# .NET build artifacts
bin/
obj/
*.csproj.user

# Old publish output (regenerate from source, don't commit)
publish/

# Per-user, per-run app data - never something to ship in the repo
SnowFixer_Output/
native-startup.log

# IDE / editor
.vs/
.vscode/
*.user
CMakeUserPresets.json

# OS cruft
Thumbs.db
.DS_Store
